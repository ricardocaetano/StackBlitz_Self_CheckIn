/**
 * Maximum number of guests per room type.
 */
export const MAX_ROOM_GUESTS = {
  single: 1,
  double: 2,
  twin: 2
};

/**
 * Price per night for each room type.
 */
export const ROOM_PRICES = {
  single: 50,
  double: 90,
  twin: 90
};
